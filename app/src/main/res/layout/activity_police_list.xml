<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".PoliceListActivity">

    <TextView
        android:id="@+id/police_text_view"
        android:text="Police Officer List"
        android:textSize="35sp"
        android:layout_marginBottom="15dp"
        android:fontFamily="@font/mastery_kingdom_font"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/police_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/police_text_view"
        android:fadeScrollbars="true"
        android:scrollbars="vertical" />

</RelativeLayout>